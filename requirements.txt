# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# ── Market data ────────────────────────────────────────────────────────────
akshare>=1.14.0            # A股行情（日线、资金流向、财报等）
yfinance>=0.2.40           # 美股行情

# ── Computation ────────────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
pandas-ta>=0.3.14b0        # 技术指标：RSI / MACD / BBands / ATR …
scikit-learn>=1.5.0
lightgbm>=4.3.0            # Pairwise ranking model (Phase 2)

# ── Database & cache ───────────────────────────────────────────────────────
psycopg2-binary>=2.9.9     # TimescaleDB / PostgreSQL driver
sqlalchemy>=2.0.30
alembic>=1.13.0
redis>=5.0.0
celery>=5.4.0              # async task queue

# ── LLM ────────────────────────────────────────────────────────────────────
anthropic>=0.30.0          # Claude API

# ── HTTP ───────────────────────────────────────────────────────────────────
httpx>=0.27,<1.0

# ── Dev / test ─────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.23.0
